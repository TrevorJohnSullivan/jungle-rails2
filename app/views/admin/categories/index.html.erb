<h1>Categories</h1>
<table class="table">
  <thead>
    <tr>
      <th>Name</th>
      <%# <th>Description</th> %> <!-- Remove or comment this line if not needed -->
    </tr>
  </thead>
  <tbody>
    <% @categories.each do |category| %>
      <tr>
        <td><%= category.name %></td>
        <%# <td><%= category.description %></td> %> <!-- Remove or comment this line if not needed -->
      </tr>
    <% end %>
  </tbody>
</table>
<%= link_to 'New Category', new_admin_category_path, class: 'btn btn-primary' %>
